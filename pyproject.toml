[project]
name = "funidiscordbot"
version = "0.1.0"
description = "A discord bot for fwiends"
requires-python = ">=3.12"
dependencies = [
    "py-cord==2.8.0rc1",
    "python-dotenv==1.2.2",
    "requests>=2.32.5",
    "wand==0.7.0",
]
